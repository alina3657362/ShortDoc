export enum AppRoute {
    Upload = '/',
    Document = '/documents/:id',
    Account = '/:nickname',
    Edit = '/:nickname/edit',
    Login = '/login',
    UserDocument = '/:nickname/documents/:id',
    Register = '/register',
}

